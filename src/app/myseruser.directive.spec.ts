import { MyseruserDirective } from './myseruser.directive';

describe('MyseruserDirective', () => {
  it('should create an instance', () => {
    const directive = new MyseruserDirective();
    expect(directive).toBeTruthy();
  });
});
