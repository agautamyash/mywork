import { ParatagDirective } from './paratag.directive';

describe('ParatagDirective', () => {
  it('should create an instance', () => {
    const directive = new ParatagDirective();
    expect(directive).toBeTruthy();
  });
});
